/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,js,jsx,ts,tsx}", // Add paths to all files that will contain Tailwind classes
  ],
  theme: {
    extend: {
      // You can add custom theme options like colors, fonts, etc. here if needed
    },
  },
  plugins: [],
}
